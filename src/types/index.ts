export type TypeServices = {
    id: number
    title: string
    description: string
    price: string
    condition: string
    image: string

}

export type TypeBenefits = {
    id: number
    title: string
    description: string
    image: string
}

export type TypeReviews = {
    id: number
    perfil: string
    name: string
    position: string
    comment: string
}